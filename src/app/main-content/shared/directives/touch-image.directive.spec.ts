import { TouchImageDirective } from './main-content/shared/directives/touch-image.directive';

describe('TouchImageDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchImageDirective();
    expect(directive).toBeTruthy();
  });
});
